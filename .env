# Tell Docker Compose to read .env.local for variable interpolation.
# This allows per-environment overrides (e.g. ADMIN_UI_HOST_PORT, ZITADEL_HOST_PORT)
# without committing secrets. Copy .env.example to .env.local to get started.
COMPOSE_ENV_FILES=.env.local
