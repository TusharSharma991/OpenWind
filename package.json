{
  "name": "platform",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.15.9"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "pretest:isolation": "node -e \"process.env.CI||require('child_process').execSync('tsx scripts/check-docker-services.ts postgres redis openbao',{stdio:'inherit'})\"",
    "test:isolation": "turbo run test:isolation",
    "pretest:e2e": "node -e \"process.env.CI||require('child_process').execSync('tsx scripts/check-docker-services.ts postgres redis openbao minio zitadel',{stdio:'inherit'})\"",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\"",
    "db:migrate": "turbo run db:migrate --filter=@platform/db",
    "db:migrate:create": "turbo run db:migrate:create --filter=@platform/db",
    "db:seed": "turbo run db:seed --filter=@platform/db",
    "db:studio": "turbo run db:studio --filter=@platform/db",
    "clean": "turbo run clean && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "ci": "pnpm commitlint --from HEAD~1 --to HEAD && pnpm format:check && pnpm lint && pnpm typecheck && pnpm build && pnpm test && pnpm check-analytics-annotations",
    "check-analytics-annotations": "tsx scripts/check-analytics-annotations.ts",
    "prepare": "husky",
    "bootstrap": "tsx scripts/bootstrap.ts",
    "setup-auth": "tsx scripts/setup-dev-auth.ts",
    "setup-bao": "tsx scripts/setup-dev-bao.ts",
    "seed:demo": "dotenv -e .env.local -- tsx scripts/seed-demo.ts",
    "reset:data": "pnpm exec dotenv -e .env.local -- tsx scripts/reset-data.ts"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@eslint/js": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "dotenv-cli": "^11.0.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.30.0",
    "eslint-plugin-no-relative-import-paths": "^1.5.0",
    "globals": "^17.6.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.3.0",
    "turbo": "^2.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.5.0"
  },
  "pnpm": {
    "overrides": {
      "typescript": "^5.5.0",
      "esbuild": ">=0.28.1",
      "hono": ">=4.12.25"
    }
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix --max-warnings=0",
      "prettier --write"
    ],
    "*.{md,json,yaml,yml}": [
      "prettier --write"
    ]
  }
}
